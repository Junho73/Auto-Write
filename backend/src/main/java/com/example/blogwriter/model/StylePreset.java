package com.example.blogwriter.model;

// Structure/tone guides only — never real post text, to avoid copying real authors' content.
public record StylePreset(String id, String label, String description) {
}
